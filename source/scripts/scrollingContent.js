document.getElementById('link').addEventListener('click', function (e) {
    e.preventDefault();
  // Using an if statement to check the class
  document.getElementById('contenu').classList.toggle('defil');
});
